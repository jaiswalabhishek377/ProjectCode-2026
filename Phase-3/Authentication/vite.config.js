import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true
      }
    }
  }
})
//Without this, your frontend and backend run on different ports and CORS errors happen
//changeOrigin: true makes the backend think the request is coming from the browser, not Vite